name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  ci:
    uses: JDE-Projects/Build-Tools/.github/workflows/ci-windows.yml@11b8f56cafce614e93f8453826251598311a31b9 # Build-Tools main (ruff, import check, pytest)
    permissions:
      contents: read
    with:
      entry_script: "simple_firearm_logbook.py"
